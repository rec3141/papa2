name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build-and-test:
    name: Build & test (${{ matrix.os }} / Python ${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      # ------------------------------------------------------------------ #
      # System-level build dependencies
      # ------------------------------------------------------------------ #
      # gcc/g++/make/zlib1g-dev ship preinstalled on ubuntu-latest images;
      # this step only tops them up and must never hang the job on a flaky
      # apt mirror (which has stalled runs for 30+ minutes).
      - name: Install system dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          timeout 180 sudo apt-get update -qq && \
          timeout 180 sudo apt-get install -y --no-install-recommends \
            g++ gcc zlib1g-dev make \
          || echo "apt unavailable; relying on preinstalled toolchain"
          g++ --version && make --version | head -1

      # ------------------------------------------------------------------ #
      # Compile shared library
      # ------------------------------------------------------------------ #
      - name: Build libpapa2.so
        run: make libpapa2.so

      # ------------------------------------------------------------------ #
      # Python package installation
      # ------------------------------------------------------------------ #
      - name: Upgrade pip / build tools
        run: python -m pip install --upgrade pip setuptools wheel

      - name: Install papa2 with test dependencies
        run: pip install ".[test]"

      # ------------------------------------------------------------------ #
      # Tests
      # ------------------------------------------------------------------ #
      - name: Run pytest
        run: pytest tests/ -v --tb=short
